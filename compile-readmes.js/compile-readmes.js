const fs = require('fs'); const path = require('path'); const outputFile = path.join(__dirname, 'compiled-n8n-docs.md'); const repoRoot = __dirname; let allDocs = []; function findReadmes(dir) {   const items = fs.readdirSync(dir);    for (const item of items) {     const fullPath = path.join(dir, item);     const stat = fs.statSync(fullPath);      if (stat.isDirectory()) {       findReadmes(fullPath);     } else if (item.toLowerCase() === 'readme.md') {       const relativePath = path.relative(repoRoot, fullPath);       const content = fs.readFileSync(fullPath, 'utf8');       allDocs.push(`## ${relativePath}\n\n${content}\n\n---\n`);     }   } }  findReadmes(repoRoot); fs.writeFileSync(outputFile, allDocs.join('\n'), 'utf8'); console.log(` ✅ Done! Compiled ${allDocs.length} README.md files into ${outputFile}`);
